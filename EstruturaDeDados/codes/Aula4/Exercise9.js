const logger = require('../../../Common/Logger');
const prompt = require('../../../Common/PromptSync/prompt');

const description = '9 - Faça um algoritmo que leia três notas de um aluno, calcule e escreva a média final deste \n    aluno. Considerar que a média é ponderada e que o peso das notas é 2, 3 e 5. Fórmula para \n    o cálculo da média final é: \n    mediafinal=(n1*2+n2*3+n3*5)/10'

function presentsExercise() { 
  
}

module.exports = {
    description,
    presentsExercise
}