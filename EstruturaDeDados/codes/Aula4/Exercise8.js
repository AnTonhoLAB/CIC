const logger = require('../../../Common/Logger');
const prompt = require('../../../Common/PromptSync/prompt');

const description = '8 - Escreva um algoritmo para ler uma temperatura em graus Fahrenheit, calcular e escrever o \n    valor correspondente em graus Celsius (baseado na fórmula abaixo): \n               C/5=(F-32)/9 \n    Observação: Para testar se a sua resposta está correta saiba que 100°C = 212F.'

function presentsExercise() { 
  
}

module.exports = {
    description,
    presentsExercise
}